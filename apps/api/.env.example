# Environment
NODE_ENV="development"  # Options: development, production, test

# Server Configuration
# Use 4000 locally so the Vite app can run on 3000 (see apps/client/.env.example API_URL).
PORT=4000
LOG_LEVEL="info"  # Options: debug, info, warn, error

# Turso / SQLite Database
# For local development: creates local.db file
DATABASE_URL="file:local.db"
# For production: use Turso remote URL
# DATABASE_URL="libsql://your-db.turso.io"
# TURSO_AUTH_TOKEN="your-turso-auth-token"

# Auth
BETTER_AUTH_SECRET="your-super-secret-key-change-in-production-min-32-chars"
BETTER_AUTH_URL="http://localhost:3000"
DEFAULT_ADMIN_PASSWORD="change-this-admin-password"
# Include every origin the admin UI is served from (comma-separated).
TRUSTED_ORIGINS="http://localhost:3000,http://localhost:5173"


# Email (Resend)
RESEND_API_KEY="re_your-resend-api-key"
EMAIL_FROM="noreply@yourdomain.com"

# Media uploads via UploadThing
 UPLOADTHING_TOKEN=""

# AI Providers (optional — AI matching is opt-in)
# Embedding provider (e.g. OpenAI, Voyage, Cohere via OpenAI-compatible API)
AI_EMBEDDING_PROVIDER_URL="https://api.openai.com/v1"
AI_EMBEDDING_PROVIDER_KEY="sk-..."
AI_EMBEDDING_MODEL="text-embedding-3-small"

# LLM provider for match ranking (defaults to embedding provider if unset)
# AI_LLM_PROVIDER_URL="https://api.deepseek.com/v1"
# AI_LLM_PROVIDER_KEY="sk-..."
# AI_LLM_MODEL="deepseek-chat"

# AI_MATCH_TIMEOUT_MS=8000
